# Підказка - STREAM UPLOAD

* Створіть writable stream для `upload.txt`.
* Передайте потік запиту у файловий стрім через `pipe`.
* Завершуйте HTTP-відповідь тільки після події `finish` у файлового стріму.
