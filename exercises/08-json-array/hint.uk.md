# Підказка - JSON ARRAY

* Перевіряйте:
  * `Array.isArray(body.numbers)`
  * кожен елемент має бути `typeof n === "number"`
* Використовуйте `reduce` для суми.
* Для порожнього масиву:
  * `count = 0`
  * `sum = 0`
  * `average = 0`
* Якщо валідація не пройдена, повертайте `422`.
