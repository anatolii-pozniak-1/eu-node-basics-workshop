# Підказка - ENV CONFIG

* Використайте: `const port = Number(process.env.PORT || 3000)`.
* Для цієї вправи не покладайтеся лише на `process.argv[2]`.
* Залиште простий маршрут `GET /` для перевірки.
