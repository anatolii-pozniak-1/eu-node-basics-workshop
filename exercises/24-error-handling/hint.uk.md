# Підказка - HANDLE PARTIAL FAILURES

* `Promise.all` завершується помилкою одразу, `Promise.allSettled` — ні.
* Розпарсьте тіло запиту як JSON-масив імен файлів.
* Розкладіть результати на `successes` і `failures`.
* API має стабільно повертати результат навіть при відсутньому файлі.
