# Підказка - FILE READ

* Використайте `fs.readFile` або `fs.readFileSync`.
* Розпарсьте через `JSON.parse`.
* Переконайтеся, що `data.json` містить об'єкт, і поверніть `res.end(JSON.stringify(data))`.
